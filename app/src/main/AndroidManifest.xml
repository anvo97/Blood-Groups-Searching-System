<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.an.bloodgroupssearchingsystem">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Blood.DetailEventActivity"></activity>

        <service android:name=".Service.EvensService" />

        <activity android:name=".View.UpdateInformation.MyProfileActivity" />
        <activity android:name=".View.News.DetailActivity" />
        <activity android:name=".View.Login.MainActivity"></activity>
        <activity android:name=".View.Login.LoginActivity"></activity>
        <activity android:name=".View.Register.RegisterActivity" />
        <activity
            android:name=".View.Menu.MenuActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Donate.PhieuDangKy"
            android:screenOrientation="landscape" />
    </application>

</manifest>